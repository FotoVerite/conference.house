  <% serializedEvents = @events.map {|e| EventSerializer.new(e).serializable_hash } %>
  <%= react_component('EventsProvider', {
      events: serializedEvents,
      filteredEvents: serializedEvents,
      currentUser: @current_user.try(:serializable_hash)
    })
  %>